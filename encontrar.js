const cliente = requiel("./cliente.jsom");

function encontrar(lista, valor){
    return lista.find((item) => item[chave]===valor);
}

const encontrado = encontrar(cliente, "nome",Izaque");

    console.log(encontrado);